<UserControl x:Class="ChildModule.View.ChildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:my="clr-namespace:ChildModule.View"
             d:DesignHeight="50" d:DesignWidth="250">

    <UserControl.Resources>

    </UserControl.Resources>
    
    <UserControl.DataContext>
        <my:ChildViewModel/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Name" Style="{StaticResource TextBlockSectionTitle}"></TextBlock>        
            <TextBox Grid.Column="1" Text="{Binding Name}"></TextBox>           
        </Grid>
    </Grid>
</UserControl>
